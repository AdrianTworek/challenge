import { HeaderSearch } from './header-search';
import { NavTabs } from './nav-tabs';

export const Header = () => {
  return (
    <header>
      <HeaderSearch />
      <NavTabs />
    </header>
  );
};
